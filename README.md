# blender-pointed-arch-curve
This Blender add-on adds a new Curve type for **customizeable** round or pointed arches (gothic style).

## Features
- Uses circles (instead of an parabola) to generate the Curve
- Customizeable `Width`, `Pointiness` and `Vertices`

## Installation
Todo
